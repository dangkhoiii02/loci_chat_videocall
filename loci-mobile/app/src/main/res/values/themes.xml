<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.Locimobile" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/brand_primary_light</item>
        <item name="colorOnPrimaryContainer">@color/brand_primary_dark</item>
        <item name="colorSecondary">@color/brand_accent</item>
        <item name="colorSurface">@color/surface_card</item>
        <item name="android:colorBackground">@color/screen_background</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/nav_background</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="Theme.Locimobile" parent="Base.Theme.Locimobile" />

    <style name="CardStyle">
        <item name="android:background">@color/surface_card</item>
        <item name="android:elevation">2dp</item>
    </style>

    <style name="RoundButton" parent="Widget.Material3.Button">
        <item name="android:background">@color/brand_primary</item>
        <item name="cornerRadius">24dp</item>
    </style>
</resources>
